name: ColorLab CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: workspace/apps/colorlab/package-lock.json
      - run: npm ci
        working-directory: workspace/apps/colorlab
      - run: npm test -- --coverage
        working-directory: workspace/apps/colorlab