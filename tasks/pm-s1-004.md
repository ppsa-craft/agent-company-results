# Task: pm-s1-004 — S1 REST API + OpenAPI Spec + Contract Tests

## Metadata
- **ID**: pm-s1-004
- **Role**: DEV
- **Status**: ready
- **App**: vn-stock-suggestion
- **Milestone**: M1-S1
- **Assignee**: dev-3
- **Depends on**: pm-s1-003 (Normalization + Redis + Postgres)
- **Spec Ref**: workspace/apps/vn-stock-suggestion/stack-vn-stock-suggestion.md, workspace/apps/vn-stock-suggestion/techlead-interface-contracts.md

## Title
Build S1 REST API with OpenAPI 3.1 Spec and Pact Contract Tests for S2 Consumers

## Description
Expose the normalized market data via a versioned REST API. Publish OpenAPI spec for consumer services (S2 Signal Engine, S3 API Gateway). Implement Pact contract tests to guarantee S1→S2 schema compatibility.

## Acceptance Criteria
- [ ] FastAPI endpoints: `GET /api/v1/market-data/{symbol}`, `GET /api/v1/market-data/{symbol}/history`, `GET /api/v1/health`, `GET /api/v1/metrics`
- [ ] OpenAPI 3.1 spec auto-generated and published at `/openapi.json`
- [ ] Request validation with Pydantic (query params: timeframe, limit, from/to timestamps)
- [ ] Response pagination for history endpoints
- [ ] Rate limiting middleware (token bucket, configurable per tier)
- [ ] Pact contract tests: verify S1 responses match S2 consumer expectations
- [ ] API documentation via Scalar/Redoc at `/docs`
- [ ] Security headers: CSP, HSTS, X-Frame-Options, Referrer-Policy

## Verification
- OpenAPI spec validates against S2 consumer contract tests
- Pact broker publishes verified contracts
- Load test: 500 req/s sustained, p99 < 200ms
- All endpoints return valid JSON per schema

## Security Notes
- Rate limiting prevents abuse
- Input sanitization on all query parameters
- No sensitive data in responses (symbols are public)
- CORS configured for S3 gateway origin only