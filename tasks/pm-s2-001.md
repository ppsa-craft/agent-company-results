# Task: S2 Project Scaffold + CI/CD + Indicators Library

- **ID**: pm-s2-001
- **Role**: DEV
- **App**: `vn-stock-suggestion`
- **Milestone**: M1-S2
- **Assignee**: dev-4
- **Depends on**: None (parallel with S1)
- **Status**: ready
- **DoD Tier**: 2 (feature)

## Acceptance Criteria
- [ ] FastAPI project structure with Redis integration created
- [ ] pytest + ruff + mypy CI configured
- [ ] TA-Lib environment setup with platform support notes
- [ ] `vn-tech-indicators` package initialized for shared library
- [ ] Property-based testing framework (`hypothesis`) configured
- [ ] README with how-to-run instructions

## Implementation Plan
1. Create `/data/workspace/apps/vn-stock-suggestion/services/s2/` directory structure
2. Initialize Python project: `pyproject.toml`, `requirements.txt`
3. Create `app/` module with FastAPI app factory
4. Set up Redis connection management for stream consumption
5. Create `indicators/` subpackage stub with interface contracts
6. Set up CI/CD: GitHub Actions with `pytest`, `ruff`, `mypy`, `hypothesis`
7. Write Dockerfile
8. Create README

## Architecture Seam
S2 internal — consumes S1 schemas from `vn-stock-schemas`, produces signals consumed by S3.

## Test Plan (for TESTER)
- Contract: scaffold builds clean, CI passes on empty project
- Happy path: pip install works, pytest discovers 0 tests
- Edge: TA-Lib installation notes for both Linux and macOS
