# Task: S1 Project Scaffold + CI/CD + Shared Schemas Package

- **ID**: pm-s1-001
- **Role**: DEV
- **App**: `vn-stock-suggestion`
- **Milestone**: M1-S1
- **Assignee**: dev-1
- **Depends on**: None (first task)
- **Status**: ready
- **DoD Tier**: 2 (feature)

## Acceptance Criteria
- [ ] FastAPI project structure created with proper module boundaries
- [ ] pytest + ruff + mypy CI pipeline configured
- [ ] PostgreSQL + Redis connection configs established
- [ ] GitHub Actions workflow file created and working
- [ ] Shared `vn-stock-schemas` package initialized with Pydantic models
- [ ] README with how-to-run instructions (works verbatim in clean checkout)

## Implementation Plan
1. Create `/data/workspace/apps/vn-stock-suggestion/services/s1/` directory structure
2. Initialize Python project: `pyproject.toml`, `requirements.txt`, `setup.py`
3. Create `app/` module with FastAPI app factory pattern
4. Set up config module with PostgreSQL (`asyncpg`) and Redis (`redis-py`) connection management
5. Create `schemas/` subpackage with initial Pydantic models (PriceData, StockSymbol, Timeframe)
6. Set up CI/CD: GitHub Actions with `pytest`, `ruff`, `mypy` steps
7. Write Dockerfile for containerized runtime
8. Create README with setup, run, test instructions

## Architecture Seam
S1 internal — produces `vn-stock-schemas` package consumed by S2, S3, S4 cross-service contracts.

## Test Plan (for TESTER)
- Contract: scaffold builds clean, CI pipeline passes on empty project
- Happy path: `pip install -e .` works, `pytest` discovers 0 tests (baseline)
- Edge: GitHub Actions triggers on PR to `apps/vn-stock-suggestion/services/s1/`
