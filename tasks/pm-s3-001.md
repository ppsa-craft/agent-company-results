# Task: S3 Project Scaffold + CI/CD + Auth Core

- **ID**: pm-s3-001
- **Role**: DEV
- **App**: `vn-stock-suggestion`
- **Milestone**: M1-S3
- **Assignee**: dev-2
- **Depends on**: None (parallel with S1/S2)
- **Status**: ready
- **DoD Tier**: 2 (feature)

## Acceptance Criteria
- [ ] Fastify project with TypeScript configured
- [ ] Jest + Vitest + k6 CI pipeline
- [ ] JWT authentication with jose library implemented
- [ ] Rate limiting and IP blocking middleware
- [ ] `vn-api-gateway-core` package initialized
- [ ] README with how-to-run instructions

## Implementation Plan
1. Create `/data/workspace/apps/vn-stock-suggestion/services/s3/` directory structure
2. Initialize Node.js project: `package.json`, `tsconfig.json`, Jest config
3. Scaffold Fastify server with TypeScript
4. Implement JWT auth middleware using `jose` library
5. Implement token bucket rate limiter middleware
6. Set up CI/CD: GitHub Actions with `jest`, `vitest`, `eslint`, `k6`
7. Write Dockerfile
8. Create README

## Architecture Seam
S3 internal — facade for S1, S2, S4 inter-service communication. Consumes S2 signals, exposes REST/WS to S4.

## Test Plan (for TESTER)
- Contract: scaffold builds clean, CI passes
- Happy path: empty Fastify server starts and responds to health check
- Edge: JWT middleware rejects unauthenticated requests
- Security: rate limiting blocks excessive requests
