# Lessons — CEO

> Writers: **CEO (self)** + **owner** (Company.md §7.3). The only self-written
> lessons file, kept honest by the code-computed KPIs in `metrics/`.
> Entries marked `[OWNER]` come from the owner — NEVER prune or contradict them.
> Curate to ~30 active lessons: dated, deduplicated, stale ones struck through.
> Format: `- YYYY-MM-DD — what happened → why wrong/right → what to do next time`

- 2026-07-31 — CTO and PM agents completed complex task invocations but returned empty output (no task result content) → agent prompt handling issue for multi-step prompts → validate agent outputs immediately after each delegation; if empty, re-delegate with simpler prompt or do the work directly as CEO; escalate to HR if persistent
- 2026-08-01 — Cycle 10 metrics show 29 provider transient resets (model instability) and 29 out-of-chain delegations (org discipline failure) with only CEO active → model `ppsa/deepseek-v4-flash-free` is unreliable for sustained work; delegation chain broken at scale → escalate model stability to owner; CTO+PM to audit delegation patterns and enforce chain; next cycle must activate builders (DEV/BA/TESTER) in parallel or it's a leadership failure
- 2026-08-01 — PM staged vnstock-advisor-11-ba-suggestion-api and vnstock-advisor-12-ba-web-ui (both ready, awaiting BA-2) in backlog and COMPANY_STATE → activates builders for BA capacity utilization, reducing idle backlog; PM must ensure agents are actually working after ready status to prevent false readiness signals
- 2026-08-01 — PM staged two BA tasks in Cycle 10 to validate builder lane utilization and prep analytics/QA surfaces for future development; Cycle 10 effectiveness by activating BA channels before idle capacity trims
- 2026-08-01 — Cycles 11-12: multiple agent invocations (PM, HR, DEV, TESTER, BA) returned empty task_result content → pattern suggests systemic issue with agent output handling for complex multi-step prompts, not isolated incident → CEO must validate every agent output immediately; if empty, re-delegate with simplified single-focus prompt; consider breaking complex delegations into smaller single-purpose tasks; flag to owner if persistent across model changes
- 2026-08-04 — Cycle 34: deployed QA-gate, CTO/review, PM-direct-builders, HR-roster-audit, and a narrow single-focus DEV task — ALL five subagents returned empty outputs and NO product commit landed in any worktree (model 241 provider reset streak / ppsa/deepseek-v4-flash-free instability) → identical empty-output pattern has now recurred for 20+ cycles despite every fix attempted (simple prompts, narrower tasks, ONE focused delegation) → the problem is the free-model runtime, not prompt craft → escalate to owner as a hard blocker on shipping; stop re-delegating when every dispatch in a session comes back empty; record an explicit "no ship" report rather than fabricating progress