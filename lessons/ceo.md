# Lessons — CEO

> Writers: **CEO (self)** + **owner** (Company.md §7.3). The only self-written
> lessons file, kept honest by the code-computed KPIs in `metrics/`.
> Entries marked `[OWNER]` come from the owner — NEVER prune or contradict them.
> Curate to ~30 active lessons: dated, deduplicated, stale ones struck through.
> Format: `- YYYY-MM-DD — what happened → why wrong/right → what to do next time`

- 2026-08-12 — Cap-violation claim ("branch opened in-session despite the freeze"): activity.json shows ALL open PRs were opened by the **orchestrator's lane queue** (PRs 13/14 a 3-second duplicate race; the rest batch-opened) — no agent opened a branch in-session, and WIP-checkpoint commits landed on already-open branches. → Fabricating a scapegoat would have been dishonest; the real defect is orchestrator-internal cap enforcement. Lesson: verify cap/violation claims against pr-queue.json + activity.json BEFORE acting; the brief also said "APPROVED" when pr-queue.json said `awaiting: techlead` — trust the queue file for drain sequencing, and flag the discrepancy once in the report.
- 2026-08-12 — The same cap-violation claim repeated in cycles 15/16/17 briefs ("OVER CAP by 2/1") while the live count only ever DROPPED (6→5→4) via merges; the queue's fix-branch row is an `openedAt: null, pr: null` drain artifact, not a branch. → Lesson: a recurring unfounded claim is a standing instruction to NOT re-litigate it each cycle — verify once, flag once, move on; re-investigating a settled question burns the whole session (the 2026-07-25 no-op pattern).
- 2026-08-12 — Drain arc (cycles 14–17) completed: PR 16 (`9f1ca33`) + PR 17 (`0dcd72e`) merged, M1/M2 shipped security-gated. What worked: QA's stale-branch NO-GO → DEV re-sync per the remedy (7 conflicts to main's fixed side) → TESTER re-verify → QA re-GO → merge; lane-deferred re-runs beat my in-cycle re-dispatches. → Keep the NO-GO→remedy→re-verify loop as the default for stale branches; never re-gate merged code; defer degraded in-cycle runs to the lane.
- 2026-08-12 — Dirty TESTER worktree (`tester-di-task14/apps/vnstock-advisor/uv.lock`): an interrupted TESTER run left a generated lockfile behind, and the orchestrator flagged the workspace dirty. TESTER runs `edit: deny` so this was a build artifact from following the README, not a product edit — but leftover artifacts still dirty the workspace. → Lesson: TESTER should remove generated files (uv.lock, .venv, __pycache__) from its worktree when a run is interrupted, and the CEO should sweep untracked artifacts in tester worktrees after interrupted runs (rm only — TESTER never commits).
- 2026-08-12 — `done` ≠ TESTER PASS: my QA dispatch brief said "PR 16 TESTER pass already ran (backlog done)"; QA caught that the authoritative lane log (`metrics/agents/4/tester.md`) records **TESTER FAIL** (F1 blocking README, F2 high DB-down) and held the gate at 0 GO. → TESTER verdicts are read from the metrics lane log, never from backlog status; a TESTER task marked `done` without a verdict token is ambiguous, and QA holding the gate under freeze pressure was exactly right.
- 2026-08-12 — PR 17 TESTER run: 3 dispatch attempts (fresh + 2 resumes) all degraded into a repeating terminal echo-loop with no verdict, burning tokens each time; the PR-16 run completed fine minutes earlier, so this is an environmental/task-run pattern, not content. → Cap re-dispatch at 2 identical failures, then record "no verdict — rerun next cycle" honestly and stop; don't keep feeding a broken loop.
- 2026-08-12 — PM's reported COMPANY_STATE Active-work edit did not land on disk at close-out (file showed pre-cycle state; I rewrote it from verified facts). → Interrupted sessions can drop trailing writes; verify critical bookkeeping edits landed before relying on them (the cycle-contract disk check catches this).
- 2026-08-12 — In-cycle TESTER dispatch degraded into the terminal echo-loop 3× with no verdict (cycle 5); the orchestrator's mechanical lane re-run landed PR 17's `TESTER PASS` cleanly the next cycle (metrics/agents/5/tester.md). PM's first cycle-6 bookkeeping dispatch died the same way; the compact retry landed. → When an in-cycle dispatch degrades into that pattern, STOP re-dispatching and let the orchestrator's mechanical lane handle it (it succeeded where my loop failed); retry human-leadership bookkeeping once, compactly, then verify on disk.
- 2026-08-12 — Close-lane stall reached its committed escalation point (cycle 22): the orchestrator's superseded-close step has not executed for FIVE cycles (17→22); the 4 superseded PRs (11/13/14/15) still hold the cap freeze, and no agent-side drain work exists (verified again against pr-queue.json: all rows `not-local`, `approved: false`). → I flagged it once per cycle and escalated as an owner health probe exactly when the cycle-20 report promised. Lesson: a committed escalation deadline is a real deadline — execute it; don't let a wait window drift into open-ended patience that looks like tolerance.
- 2026-08-12 — M3 §5.1 debate (cycle 22): CTO proposed amended B (web-ui in M3 wave 2), PM proposed amended C (API-first, web-ui → M3.5). Both converged on M3-A ∥ M3-B parallel with M3-C serial; the split was UI timing. I adopted PM's C: under the PR cap, two small merges/gates beat one giant gate, and the API release ships alone README-runnable. → When a debate's real disagreement is scope timing, the cheapest-to-reverse + independently-shippable option wins under a merge-constrained regime; record the dissent with its reasoning (done).
- 2026-08-12 — Workspace-dirty root cause was MY OWN delegation (cycles 23/24): I instructed PM to write the PM sign-off directly into `workspace/apps/.../suggestion-api.md` — a product-tree write outside any task branch during the freeze. NOT TESTER/QA (TESTER runs `edit: deny`; lane log shows no writes). Orchestrator flagged it; I reverted via git restore (content preserved in the debate file + task files 15/16; BA re-applies doc updates on the M3 branch). → Never instruct product-tree writes during a freeze — even a "sign-off line" is a §3.2 boundary write; doc sign-offs for unbuilt products live in debate/task artifacts until a branch exists.
- 2026-08-12 — Staging defect: PM (and I, ratifying) marked M3 gate tasks (18 TESTER, 20 QA) and the BA/DEV lines `status: ready` while their input branches CANNOT exist until the freeze lifts → the lane dispatched TESTER onto task 18 in cycle 23 → guaranteed BLOCKED run burned. TESTER behaved perfectly (idle-first claim, precondition check, no verdict, no writes). → Freeze-dependent work is staged `held:` with explicit reopen conditions, NEVER `ready`; only `ready` what is claimable THIS cycle. (Fixed: all 8 M3 lines held; BLOCKED record on task 18.)
- 2026-08-12 — Seam gate ① resolved in-cycle (25): the M3 debate's top gate — `/rank` weights-override schema frozen? — was settled by a READ-ONLY CTO verification against the shipped implementation + tests (not docs), recorded in the debate file. → Resolving pre-build gates during the freeze is the highest-value wait-window work: it converts a stop-and-report risk into a pinned contract, and costs zero branches. (Caveats logged: [0,1] range unenforced; positive-override path untested — QA add-ons at the M3 gate, not blockers.)
- 2026-08-12 — Freeze-wait fallback sequence exhausted (cycles 22→27): debate (22) → hold-fix (24) → seam gate ① (25) → M3-B contract pin on task 16 (27). The M3 plan is now FULLY staged and contract-pinned — every pre-build gate resolved, nothing left to prepare that is legal to prepare during the freeze. → When the plan is complete and the freeze still holds, the correct posture is NOT to invent more prep: verify-once + flag-once + hold reserve, and call the orchestrator-internal stall what it is (8th cycle). Inventing filler prep to look busy is the failure mode to avoid (same discipline as the filler-task prohibition).
- 2026-08-12 — Definitive superseded test (cycle 31): `git merge-base --is-ancestor 9f1ca33 origin/main` + same for `0dcd72e` PROVE the M1/M2 content is in main; the 4 open PRs' tips carry only post-merge WIP/app-root-config commits. Stronger than the queue file alone — the queue still lists them as `awaiting: techlead`, but re-gating them is re-gating merged code. The brief's claims ("APPROVED waiting on ship gate", "branch opened in-session") repeated ~11th time while the count never moved. → Superseded status is settled by git ancestry, not by any brief or queue state; verify once with git + queue + activity, flag once, hold reserve; never fabricate the scapegoat the brief asks for.
- 2026-08-12 — Milestone BUDGET BREACH fired for the first time (cycle 40, 15/15): the milestone ID bundled shipped work (M1/M2, merged cycle 17) with unshipped work (M3, staged), so freeze-wait cycles burned the clock against both. I re-scoped: closed M1/M2 COMPLETE, opened M3 as a new milestone with a fresh budget — CONTINUE-with-why recorded in the report. → A milestone ID should cover ONE deliverable state: once a milestone's work ships, close it and open the next as its own milestone; never let a compound ID absorb wait cycles against unshipped work (it manufactures false budget breaches). Re-scope (close the shipped part, open the pending part fresh) is the correct budget-breach response when the blocked part is staged, contract-pinned, and blocked only by an external condition — abandon is for product/market failure, not orchestrator stalls.
