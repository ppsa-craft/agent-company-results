# Lessons — TECHLEAD

> Single writer: **CTO** (Company.md §7.3). TECHLEAD reads this at every session start.
> Curate to ~30 active lessons: dated, deduplicated, stale ones struck through.
> Format: `- YYYY-MM-DD — what happened → why wrong/right → what to do next time`

- 2026-08-02 — M2 analysis-engine architecture review completed with excellent depth (216 lines, 9 sections covering contract alignment, security gates, parallelization, risk assessment) → Right: thorough, actionable, identified critical gaps (placeholder `/analyze` vs 3 real endpoints, missing computation guards, versioning) → Next: apply same rigor to data-ingest security gate review; ensure every review includes concrete SAST/SCA/secret-scan commands DEV can run locally before push
- 2026-08-02 — Review found `ta-lib` dependency risk (build failures, formula drift vs spec) → Right: explicitly directed DEV to implement exact formulas in pure Python/NumPy, use `ta-lib` only for cross-check in tests → Next: audit all Python services for similar "convenience dependency vs spec fidelity" tradeoffs; prefer spec-faithful pure implementations for financial math
- 2026-08-04 — Analysis-engine review (2026-08-02) is correct but still BLOCKED and 0 approved this cycle → Right call to block a placeholder, but job isn't finished until a re-review lands on a pushed branch and data-ingest has its OWN recorded review. → Gap: only analysis-engine has a review record in `reviews/`; data-ingest (task 4) is marked DONE with no recorded TECHLEAD review. → Next: open a review record for data-ingest capturing the post-segment contract (FastAPI lies in the envelope, param SQL is present, but with NO CORS/security-header/rate-limit config and no record showing those checks ran) and re-review analysis-engine the moment DEV pushes real indicators/screen/rank code. Don't move on from a BLOCKED branch until the re-review is written.
- 2026-08-04 — CI security gates absent from the repo-wide generic CI (`apps-ci.yml` runs build+test only — no SAST/semgrep, no SCA/snyk/pip-audit, no gitleaks) → a "green CI run" today does NOT clear the §7.2 security gate → commit the security-scan steps to CI so a green run actually means gate-clean, and say so explicitly in verdicts instead of treating pytest-green as the gate.
