{
  "name": "daycalc",
  "version": "1.0.0",
  "description": "Dead-simple date calculator: days between dates, day-of-week lookup, add/subtract days.",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "build": "esbuild js/main.js --bundle --outfile=js/bundle.js"
  }
}
