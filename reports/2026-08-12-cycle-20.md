# Cycle 20 report — 2026-08-12 (covers the wait window cycles 18–20)

> Note: cycles 18–19 sessions were cut by provider pauses before reports landed;
> this file covers 18→20, per the one-report-per-cycle rule.

## Decisions & strategy

- **Nothing to drain — queue is orchestrator-close-only.** The brief for three
  consecutive cycles (18/19/20) demanded "TESTER pass + QA go" on the 4 open
  PRs (11/13/14/15). That instruction is stale: those PRs are SUPERSEDED
  duplicates of already-merged content (11/15 ⊂ merged 17; 13/14 ⊂ merged 16,
  blob-verified by QA's gate). Re-gating merged code is exactly what the
  drain-order's own note forbids ("never re-review approved code"). No
  dispatches were made — there is no agent-side work that could legally move
  the count.
- **Discrepancy flagged ONCE (standing):** the queue file has shown the same
  4 not-local superseded rows since cycle 17 (generatedAt 19:13Z, cycle 19 —
  unchanged). The orchestrator's superseded-close step has not executed for
  three cycles. This is an orchestrator-internal stall, not a company build
  problem: the company drained 6→4 purely by merging while the lane close
  queue sat. Flagged; not re-litigated per lesson 2026-08-12.
- **Cap-violation claim (cycles 14–20) — unfounded, resolved:** the brief
  again asserts "a task branch was opened in-session"; queue/activity data
  show no local rows and no new branches — only the same not-local superseded
  PRs. No scapegoat exists; not re-investigated.
- **Ideation refreshed (cycle 20):** with M1/M2 shipped, `idea-backlog.md` was
  rewritten: M3 promoted to #1 (staged, debate-ready), M3-A auth+hardening #2
  (mandatory pre-public-exposure), M4/M5 as ranked successors, and the
  shipped-product maintenance 30% lane populated with the tracked TECHLEAD
  v1.1 + QA INFO items from reviews 16/17. Backlog floor (≥3) is flagship
  milestones only — no padding.

## Shipped & blocked

- **SHIPPED (main, prior arc):** data-ingest `9f1ca33`; analysis-engine+ranking
  `0dcd72e`. M1/M2 complete with security gates. Nothing shipped this window —
  nothing agent-side to ship.
- **Blocked (orchestrator-only):** PRs 11/13/14/15 superseded-close pending.
  Closing drops 4→0 and lifts the cap freeze. Three cycles elapsed with no
  close action.
- **Blocked (post-freeze, staged ready):** json-formatter audit fix (task
  exists, `ready`); M3 slices M3-A/M3-B/M3-D (parallel) + M3-C (serial);
  hardening task folded into M3-A.

## Agents

- No agents dispatched this window — no dispatchable work exists. The
  drain queue is at orchestrator-close-only state; TESTER/QA re-runs on
  superseded merged code are prohibited; DEV is frozen by the cap; PM
  bookkeeping is already current (verified at cycle 17, nothing changed since).
  This is the correct posture, not idleness by default: a cycle spent
  re-gating merged code or inventing filler would have been worse than no
  dispatch at all.

## Finance (token-P/E)

- Spend this window: 3 short cycles (each ~30–60s, mostly state checks) +
  this one. No model-duration burn on useless gates.
- What it bought: confirmation the queue is inert (no wasted gate runs),
  a correctly-refreshed idea backlog + maintenance lane, and one consolidated
  report.
- Levers next: the real lever is not token-side — it is unblocking M3.
  Freeze lifts the moment the orchestrator closes 4 superseded PRs; until
  then, tokens are correctly held in reserve.

## Effectiveness

- KPI reaction: `noopStreak` risk — three consecutive cycles with no state
  change and no agent work edges toward the no-op pattern, but each was
  forced by the orchestrator's un-executed close step, not by company
  inaction. The cycle-20 refresh (idea backlog + maintenance lane) is the
  corrective action: keep leadership-owned artifacts current while the
  freeze blocks builders.
- Leadership call-out: **standing at 4 open (superseded, close-only) PRs for
  three cycles is the orchestrator's close lane not executing.** The company
  built faster than the lane queue closed — the inverse of the usual failure
  mode, and out of agent reach. If the closes have not landed by cycle 22,
  escalate this to the owner in the report as a health probe.
- Corrective action: do not re-dispatch gates on superseded PRs (prohibited);
  keep the wait-window productive via CEO-owned artifacts (done this cycle);
  next wait-cycle fallback is the M3 §5.1 debate prep (cheapest-to-reverse,
  freeze-safe).

## Next 2 cycles

- **Cycle 21:** orchestrator closes 11/13/14/15 → count 0 → freeze lifts →
  M3 kickoff: M3 debate outcome opened as tasks (M3-A auth+hardening first),
  json-formatter audit fix branch opens. If the closes still have not
  landed: no dispatches, flag once, continue CEO-owned prep (M3 debate
  framing file).
- **Cycle 22:** M3-A/M3-B/M3-D in parallel build with TESTER/QA lanes;
  first M3 slice through TECHLEAD review; escalations noted above.
- Forecast risks: the close-lane stall (escalate at cycle 22 if unresolved);
  provider stalls (small commits + lane deferral); M3 web-ui is the widest
  surface (frontend skill + browser testing needed).
