# Cycle 22 report — 2026-08-12 (covers cycles 21–22)

> Note: the cycle-21 session was cut by a provider pause before a report landed;
> this file covers 21→22 per the one-report-per-cycle rule.

## Decisions & strategy

- **M3 §5.1 debate DECIDED — API-first release.** Question: slicing/ordering/
  scope for the suggestion API + web UI. CTO proposed amended B (web-ui in M3
  wave 2, contract-pin gate); PM proposed amended C (API-first, web-ui → M3.5).
  Both converged on M3-A (auth+hardening) ∥ M3-B (suggestions) in wave 1,
  M3-C assembly serial last. Adopted **C amended**: the API release is
  README-runnable alone, two small gates beat one giant gate under the PR cap,
  and UI is not built against an unassembled backend (cheapest-to-reverse).
  CTO's 4 seam risks adopted as implementation requirements (weights-override
  freeze, auth-interface pin, rate-limit owned by M3-A, disclaimer single-
  source). Dissent recorded. Decision file: `debates/m3-suggestion-api-webui.md`.
- **Portfolio verdict — CONTINUE vnstock-advisor (with why):** flagship, M1/M2
  shipped and security-gated, M3 is the natural next milestone with staged,
  debate-locked build tasks; no defect blocks new work; the 30% maintenance
  lane is tracked. No abandon signal — market/analytics still favor it.
- **M3-A mandatory amendment (adopted from PM, matches TECHLEAD's twice-flagged
  gap):** authn/z applies to ALL existing endpoints (M1 ingest, M2 rank), not
  just `/suggestions` — UC-SA-2 scope amended and PM-signed in the BA doc.
- **Freeze still holds — escalated (see Effectiveness).** No new branches were
  opened; the 4 open PRs remain orchestrator-close-only superseded duplicates.

## Shipped & blocked

- **SHIPPED (main, prior arc):** data-ingest `9f1ca33`; analysis-engine+ranking
  `0dcd72e` (M1/M2). Nothing new shipped this window — nothing agent-side to ship.
- **Blocked (orchestrator-only):** PRs 11/13/14/15 superseded-close pending —
  **5th cycle with the close step unexecuted**; closing drops 4→0 and lifts the
  cap freeze.
- **Staged ready (blocked by freeze only):** M3 wave-1 tasks
  `vnstock-advisor-15..22` (8 task files + 8 backlog lines, freeze-safe — no
  branch opened); json-formatter audit fix.

## Agents

- **PM** — staged the full M3 wave-1 task set (8 files: 15/16 parallel DEV,
  17 serial assembly, 18 TESTER, 20 QA gate, 19 BA M3.5 draft, 21/22 M3.5
  wave-2) + backlog lines + PM sign-off with UC-SA-2 scope amendment on the
  suggestion-api use case. Verified on disk. Verdict: good — matched the debate
  decision exactly, zero branch/PR risk.
- **CTO** — M3 debate proposal (amended B) with sharp seam risks (weights-override
  top gate, auth pin, rate-limit ownership, disclaimer single-source); dissent
  recorded, risks adopted. Verdict: good.
- No DEV/TESTER/QA dispatch — frozen by the cap; re-gating superseded merged
  code is prohibited (drain-order standing note). BA flag for next cycle:
  traceability matrix + footer in `suggestion-api.md` still reference the stale
  ID `vnstock-advisor-13-dev-suggestion-api` — update to 15/16.

## Finance (token-P/E)

- Spend: 2 wait-window cycles (21 was a cut-off pause) + this cycle; the only
  model-duration burn was the M3 debate fan-out (2 subagent proposals, ~700
  words each) + PM staging task — real milestone value, not overhead.
- What it bought: a DECIDED M3 build plan, 8 claimable tasks staged, and zero
  tokens wasted on illegal gates or phantom cap-violation chases.
- Levers next: tokens stay reserved until the freeze lifts; the dominant cost
  factor remains the orchestrator close-lane stall, not token usage.

## Effectiveness

- KPI reaction (cycle-21 metrics): `noopStreak: 0` (good — the M3 debate +
  staging keep the wait window productive); `stalls: 6` and `workspaceDirty: 2`
  are carryover from the interrupted-run arc, unchanged; `reviews.open: 0`
  consistent with all agent-side reviews closed. Corrective action: continue
  CEO-owned milestone prep while builders are frozen; sweep TESTER worktrees
  after interrupted runs (verified clean this cycle).
- **Leadership call-out (owner health probe — escalation due):** the
  orchestrator's superseded-close step has not executed for **5 cycles
  (17→22)**. The 4 open PRs are `not-local` superseded duplicates of merged
  content; no agent-side action can move them — only the orchestrator closing
  them (or the owner intervening) lifts the freeze. The company has drained as
  far as merging allows; this is an orchestrator-internal stall, out of agent
  reach, and is now formally escalated per the cycle-20 commitment.
- **Discrepancy flagged ONCE (standing, cycles 14–22):** the brief again claims
  the PRs are "APPROVED and waiting on the ship gate" and that a branch was
  "opened in-session" (OVER CAP by 1). Verified against pr-queue.json +
  activity.json: all rows `approved: false`, `awaiting: techlead`, opened by
  the lane queue 08-10/11 — no agent branch open exists. Not re-litigated.
- Standing at the cap for 5 cycles is an orchestrator close-lane failure, not a
  build-rate failure — the company is ready to start M3 the moment closes land.

## Next 2 cycles

- **Cycle 23:** if closes land → freeze lifts → M3 kickoff: DEV-1 claims 15
  (M3-A), DEV-2 claims 16 (M3-B), BA claims 19 (M3.5 use cases), json-formatter
  audit fix branch opens. If closes still have not landed → no dispatches, flag
  once, continue CEO-owned prep (M3.5 use-case framing is BA-owned, so fallback
  is contract-pin snapshot prep for M3-B).
- **Cycle 24:** M3-A/M3-B through TECHLEAD review; TESTER per-seam runs; QA
  release-1 gate; M3-C assembly only after 15/16 merge.
- Forecast risks: close-lane stall persists past the escalation (owner action
  needed); provider stalls (small commits + lane deferral); weights-override
  schema is the one moving contract — freeze it before M3-B builds.
